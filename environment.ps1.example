<#
  Local environment setup for MEI-Mentor (Windows / PowerShell)
  This file is ignored by Git (.gitignore) and is meant ONLY for local development.

  Usage (PowerShell):

    Copy this file to environment.ps1 and fill in your actual API keys
    cp environment.ps1.example environment.ps1

    cd backend
    .\environment.ps1
    mvn spring-boot:run -Dspring-boot.run.profiles=dev

  After running this script, the variables will be available in the current
  PowerShell session and picked up by Spring Boot via application.yml.
#>

# AbacatePay - Sandbox / Dev Mode
$env:ABACATEPAY_BASE_URL = "https://api.abacatepay.com/v1"
$env:ABACATEPAY_API_KEY  = "your_abacatepay_api_key_here"

Write-Host "✅ Environment variables set for AbacatePay (local development)." -ForegroundColor Green
Write-Host "   - ABACATEPAY_BASE_URL = $($env:ABACATEPAY_BASE_URL)"
Write-Host "   - ABACATEPAY_API_KEY   = **** (hidden)" -ForegroundColor Yellow

$env:OPENAI_API_KEY = "your_openai_api_key_here"
# opcional, se quiser customizar:
$env:OPENAI_MODEL = "gpt-4o-mini"
$env:OPENAI_TEMPERATURE = "0.0"
$env:OPENAI_MAX_TOKENS = "10"
